"""Market data from the open web, without any API key: shop and classified pages that expose
standard product data (schema.org JSON-LD, OpenGraph), found through a public search engine.
Polite by design: robots.txt is honoured, blocked hosts are left alone, nothing is bypassed."""
